"""Конфигурация приложения ``accounts``."""

from django.apps import AppConfig


class AccountsConfig(AppConfig):
    """Описывает конфигурацию приложения пользовательских учётных записей.

    На первом этапе приложение содержит только каркас, необходимый для
    будущей реализации аутентификации, ролей и seed-механики. Класс нужен
    Django для регистрации приложения и его метаданных.

    Параметры:
        Явные параметры не требуются. Экземпляр создаётся инфраструктурой
        Django автоматически.

    Возвращает:
        Экземпляр конфигурации приложения ``AppConfig``.

    Исключения:
        Специальные исключения не генерируются.

    Побочные эффекты:
        Регистрирует приложение в реестре Django.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.accounts"
    verbose_name = "Учётные записи"
