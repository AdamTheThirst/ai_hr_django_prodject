"""Временные представления приложения ``accounts`` для каркаса проекта."""

from django.views.generic import TemplateView


class LoginPlaceholderView(TemplateView):
    """Отображает заглушку будущего экрана входа.

    Представление нужно только для подготовки базовой маршрутизации и
    проверки структуры проекта до реализации полноценной аутентификации.

    Параметры:
        Явные параметры не принимаются. Django использует стандартные
        параметры ``TemplateView``.

    Возвращает:
        ``TemplateResponse`` с HTML-страницей-заглушкой.

    Исключения:
        Возможны стандартные исключения рендеринга Django.

    Побочные эффекты:
        Побочные эффекты отсутствуют.
    """

    template_name = "accounts/login_placeholder.html"
