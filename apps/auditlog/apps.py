"""Конфигурация приложения ``auditlog``."""

from django.apps import AppConfig


class AuditLogConfig(AppConfig):
    """Описывает конфигурацию модуля технического логирования.

    Приложение резервирует архитектурное место под будущие технические и
    административно значимые логи, которые будут храниться в базе данных.

    Параметры:
        Явные параметры не требуются.

    Возвращает:
        Экземпляр ``AppConfig`` для приложения ``auditlog``.

    Исключения:
        Специальные исключения не генерируются.

    Побочные эффекты:
        Регистрирует приложение в проекте Django.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.auditlog"
    verbose_name = "Технический журнал"
